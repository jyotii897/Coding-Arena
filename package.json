{
  "name": "coding-arena-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run start --prefix server",
    "build:client": "npm run build --prefix client"
  }
}
